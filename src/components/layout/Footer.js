import React from "react";
import PropTypes from "prop-types";

const Footer = props => {
  return (
    <div>
      <p>hello form footer</p>
    </div>
  );
};

Footer.propTypes = {};

export default Footer;
