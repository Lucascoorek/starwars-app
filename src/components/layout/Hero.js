import React from "react";
import PropTypes from "prop-types";

const Hero = props => {
  return (
    <section className="hero">
      <h1>power source</h1>
    </section>
  );
};

Hero.propTypes = {};

export default Hero;
